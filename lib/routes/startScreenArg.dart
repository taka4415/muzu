// ignore: file_names
class StartScreenArguments {
  final snap;
  final season;
  final episode;
  final episode_title;
  final title;
  StartScreenArguments(
      this.title, this.season, this.episode, this.episode_title, this.snap);
}

// https://docs.flutter.dev/cookbook/navigation/navigate-with-arguments